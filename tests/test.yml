---
- hosts: localhost
  remote_user: root
  collections:
    - startxfr.client
- name: Testing the startxfr client collection
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  roles:
    - name: Execute the ocp role with create option
      role: startxfr.client.ocp
      vars:
        sc_ocp_action: "create"
      tags: 
        - create
    # - name: Execute the ocp role with delete option
    #   role: startxfr.client.ocp
    #   vars:
    #     sc_ocp_action: "delete"
    #   tags: 
    #     - delete